name: pension_compass
description: "연금나침반 - ISA 연금저축 세금 계산기 & 인출 시뮬레이터"
publish_to: 'none'
version: 1.2.0+5

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # 상태 관리
  flutter_riverpod: ^2.6.1
  
  # 차트
  fl_chart: ^0.69.2
  
  # 로컬 저장
  shared_preferences: ^2.3.5
  
  # 숫자 포맷 (천 단위 콤마 등)
  intl: ^0.20.2
  
  # 인앱 리뷰
  in_app_review: ^2.0.10
  
  # 광고 (9.0.0: 이 프로젝트의 Gradle 9.1/AGP 9.0.1 기본 툴체인과 호환되는 유일한 버전.
  # gongsi-hanjul 검증판 5.3.1은 구형 Groovy `configurations.all` 문법이 Gradle 9 API와
  # 충돌해 `flutter build apk` 자체가 실패함 — 실측 후 9.0.0으로 전환. compileSdk 36 지원.)
  google_mobile_ads: ^9.0.0
  share_plus: ^13.2.1
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  
  # 앱 아이콘 생성
  flutter_launcher_icons: ^0.14.3

# 앱 아이콘 설정 (공시한줄과 동일: adaptive icon 미사용)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon.png"
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
